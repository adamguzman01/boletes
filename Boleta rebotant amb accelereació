int x=0;
int dx=2;


void setup() {
  size(500,200);
}
void draw() {
  background(200);
  ellipse(x,100,50,50);
  fill (250,0,0);
  x=x+dx;
  if (x > 500) {
   dx=-2;
  }
  if (x < 0) {
    dx=+2;
 }
 if (x==250) {
   dx=dx*2;
 }
}
